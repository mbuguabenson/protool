# XML Uploads Public Directory

This folder is populated automatically from the project-root `xml-uploads/` directory when `npm start` or `npm build` runs.

Drop XML files into the root `xml-uploads/` folder and the manifest generator will copy them here for the app to serve.
